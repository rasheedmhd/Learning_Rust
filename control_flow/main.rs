// CONTROL FLOW
// if requires a boolean to compare
// when you fail to provide a bool to if, your code won't compile
// if won't automatically coerce a value to a boolean to compare
// we must always explicitly return a boolean to if
// we can use optional else to provide default code to execute when if fails
//fn main() {
//    let number = 7;
//
//    if number < 5 {
//        println!("condition was true");
//    } else {
//        println!("condition was false");
//    }
//
//}

